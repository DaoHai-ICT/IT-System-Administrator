*** Nội dung dành riêng cho file script restart network adapter ***

- Cần phải kiểm tra trước card wifi đang là WI-FI hay Wi-Fi 2 vì thực tế nếu thiết bị đã được thay card wifi thì có thể sẽ đổi sang Wi-Fi 2

- Nếu như là Wi-Fi 2 hoặc số nào đó khác thì phải chỉnh sửa code script phần "Wi-Fi 2" nằm trong dấu " " 

- Bắt buộc phải chạy dưới quyền administrator

- Copy File Script vào ổ C:\